<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/styles.css') }}">
    <title>Suspicious File Detected</title>
    <script>
        // Redirect to index after 5 minutes
        setTimeout(function() {
            window.location.href = '{{ url_for("remove_suspicious_page") }}';
        }, 300000);
    </script>
</head>
<body>
    <div class="container">
        <h1>Suspicious File Detected</h1>
        <h2>File: {{ result['file_name'] }}</h2>
        <p><strong>Malicious:</strong> {{ result['isMalicious'] }}</p>

        <h3>VirusTotal:</h3>
        <pre>{{ result['virus_total'] | tojson(indent=2) }}</pre>

        <h3>Hybrid Analysis:</h3>
        <pre>{{ result['hybrid_analysis'] | tojson(indent=2) }}</pre>

        <h3>MetaDefender:</h3>
        <pre>{{ result['metadefender'] | tojson(indent=2) }}</pre>

        <h3>Jotti:</h3>
        <pre>{{ result['jotti'] | tojson(indent=2) }}</pre>

        <h3>CLEAN MX:</h3>
        <pre>{{ result['cleanmx'] | tojson(indent=2) }}</pre>

        <p>Page will automatically redirect after 5 minutes.</p>
    </div>
</body>
</html>
